# Core Deep Learning Backend
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Multi‑Modal Vision & Point Cloud Processing
open3d>=0.18.0          # LiDAR point cloud handling
opencv-python>=4.9.0    # Camera image preprocessing
numpy>=1.26.0
scipy>=1.12.0
pillow>=10.2.0

# Transformer / Cross‑Attention Modules (Fastformer, Cross‑Modal Attention)
transformers>=4.38.0
einops>=0.7.0           # For attention reshaping
timm>=0.9.12            # Pathlet/Fastformer vision encoder backbones

# GDFN (Gated‑Directional‑Feed‑Forward) & MLP Layers
ml-collections>=0.1.1
pyyaml>=6.0.1
tqdm>=4.66.2

# Visualization & Evaluation
matplotlib>=3.8.3
tensorboard>=2.16.2
wandb>=0.16.4           # Optional: experiment logging

# Data I/O & Augmentation (Noise Module for Camera/LiDAR noise injection)
albumentations>=1.4.0
h5py>=3.10.0
pandas>=2.2.1

# Model Export & Deployment (Optional)
onnx>=1.15.0
onnxruntime>=1.16.3
